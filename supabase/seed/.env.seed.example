# Environment for supabase/seed/seed.mjs — DEVELOPMENT ONLY.
#
# NEVER commit a real service_role key. This file is a template; copy it
# to `.env.seed.local` (already gitignored) and fill in real values only
# on your own machine, never in a deployed environment.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional — password used for all seeded demo accounts.
ALERTA_DEMO_PASSWORD=AlertaDemo123!
